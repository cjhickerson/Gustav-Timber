<picture>
	<source srcset="{{post.thumbnail.src('medium')}}" media="(max-width: 360px)">
	<img class="fit round"
		{% if picsize == 'tease' %}
		src="{{post.thumbnail.src('thumbnail')}}"
		{% else %}
		src="{{post.thumbnail.src('medium')}}"
		{% endif %}
	/>
</picture>
